import React from 'react';
import { render } from '@testing-library/react';
import BusInfoCarousel from '../BusInfoCarousel';

test('The BusInfoCarousel is rendering', () => {
  render(<BusInfoCarousel />);
});
