import React from 'react';
import { render } from '@testing-library/react';
import FormAndSuggestionsContainer from '../FormAndSuggestionsContainer';

test('The FormAndSuggestionsContainer is rendering', () => {
  render(<FormAndSuggestionsContainer />);
});
