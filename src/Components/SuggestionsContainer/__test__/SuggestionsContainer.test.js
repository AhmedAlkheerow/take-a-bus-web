import React from 'react';
import { render } from '@testing-library/react';
import SuggestionsContainer from '../SuggestionsContainer';

test('renders the SuggestionsContainer', () => {
  render(<SuggestionsContainer />);
});
